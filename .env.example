# AI Robo Advisor Environment Configuration (Example)
#
# Usage:
# 1) Copy this file to .env
# 2) Replace placeholder values (your-*-api-key) with your actual keys
# 3) Keep this file out of version control (already ignored)
#
# Security: API keys are REQUIRED and must not be committed. Non-sensitive defaults are provided below.

# ===== Required: Data Provider =====
# Polygon.io is required for financial data
# Get your API key from: https://polygon.io/
POLYGON_API_KEY=your-polygon-api-key

# ===== Required: At least one LLM Provider =====
# Provide at least one of the following keys

# Google Gemini
# Get your Google API key from: https://ai.dev/
GOOGLE_API_KEY=your-google-api-key

# OpenAI GPT
# Get your OpenAI API key from: https://platform.openai.com/
OPENAI_API_KEY=your-openai-api-key

# Anthropic Claude
# Get your Anthropic API key from: https://anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key

# ===== Optional: Additional Providers =====
# Groq (optional)
# Get your Groq API key from: https://groq.com/
GROQ_API_KEY=your-groq-api-key

# ===== Non-sensitive Defaults (Safe) =====
# These defaults reduce warnings and improve DX. They are safe to keep in version control.
ENVIRONMENT=development
PYTHONDONTWRITEBYTECODE=1
PYTHONUNBUFFERED=1
# PYTHONPATH includes both app root and src for robust module resolution in all stages/containers
PYTHONPATH=/app:/app/src

# ===== Optional App Settings (Uncomment and adjust as needed) =====
# LOG_LEVEL=INFO
# DEFAULT_LLM_MODEL=gemini-2.5-flash
# ENABLE_DETAILED_ANALYSIS=true
# MAX_PORTFOLIO_SIZE=10
# API_RATE_LIMIT=60
# API_TIMEOUT=30
